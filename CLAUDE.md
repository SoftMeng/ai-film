# AI Film · 项目级上下文

## 工程身份

- **目的**：为 AI 视频生成模型编写 10 秒竖屏片段提示词
- **默认平台**：Seedance（提示词默认中文）
- **底层规范**：默认按 Seedance 中文结构；用户明确要求英文 h3 三段式时切到 `模板/h3-Subject1-模板.md`

## 入口指令

`指令/主指令.md`（用户唯一入口）

## 工程铁律（最高优先级）

### 【铁律 1 · 单一职责】

工程设计与架构遵循单一职责原则。极力避免：职责不清晰、越界、混合、混淆、命名混乱、引用混乱、归属不清晰、边界抽象、逻辑重复、异常静默处理。

目标：正确性、可读性、可维护性、性能、安全。

严禁态度：打补丁、敷衍、捏造、臆测、留给下次处理。

### 【铁律 2 · 整洁之道】

文档篇幅不是越大越好，而是越精炼有效越好。

- 不敷衍，也不过度设计
- 追求简洁高效
- 警惕"加得越多错的越多"

判断标准：能用一段说清楚的不写两段；能用一行说不写两行；能用机制解决的不要用条款约束。

### 【铁律 3 · 禁止写入元信息】

严禁将任何元信息写入工程文件。元信息 = **非内容性**的数据，包括但不限于：

- ❌ 顶部 `//` 注释块（题材 / 风格 / 抽中维度清单 / NNN 编号 / 抽样结果等）
- ❌ `## 元信息 / 源信息 / 文件说明 / 用途 / 维护规则 / 沉淀规则 / 来源说明` 等标题段
- ❌ `> 维度：N` 顶部标注行
- ❌ 表格化的"类型 / 字数 / 来源 / 沉淀日期 / 字节 / 长度" 等元数据块
- ❌ "本文件用于 X / 沉淀自 Y / 由 Z 创建 / 维护者 W / 创建日期 / 最后更新"
- ❌ 任务背景 / 抽中维度清单 / 抽样结果

**不是元信息**（可保留）：

- ✅ 内容本身的标题、一句话摘要、概念定义
- ✅ 字段值（如 `## 一、美学方向` `## 二、颜色基调` 等内容结构）
- ✅ 严禁项（规则内容，不是元信息）
- ✅ 变体钩子（使用说明，不是元信息）
- ✅ 链接路径（如 `化妆间/*.txt` 是引用，不是元信息）
- ✅ 表格内是数据内容本身（不是元数据）

**理由**：文件本身已自含内容；元信息是冗余噪音，且易随时间过期。文件名已隐含 NNN / 来源等标识，无需在内容中重复。

### 【铁律 4 · 产物对外接口纯净】

产物（`创意MV/NNN.txt`）是给网站 / 编辑软件 / 真人导演等陌生人消费的纯描述文本，**内部代号零容忍**：

- ❌ 化妆间文件名（No0008、No0011 等）
- ❌ 镜头模板编号（"模板 2"、"模板 5"）
- ❌ 节奏组合名（"嘉桐摇"、"香菜摇"、"手枪连招"）
- ❌ 原子动作代号（"胯画八"、"抬手画圈"、"顶胯定格"）
- ❌ 内部文件路径（`镜头叙事库/xxx.md`、`舞蹈库/xxx.md`、`化妆间/NoXXXX.txt`）

抽样时 AI 可读项目内部资源；**写作时必须把抽样结果展开为描述性内容**——运镜直接描述（"Static + 微晃"）、动作直接描述（"胯部画 8 字"）、节奏直接描述（"中速律动 BPM 100-120"）、装扮直接描述（颜色 + 材质 + 形制）。

**理由**：产物最终消费者是网站，网站只能解析描述性文本。内部代号是项目自身索引，AI 抽样时用，写作时必须脱敏。这是 v3.2 修复产物不可用的根本约束。

## 关键资产位置

| 资产 | 路径 |
|---|---|
| 形象（人设）| `虚拟演员/<名字>.md` |
| 装扮（视觉）| `化妆间/<人物形象>.txt` |
| 美学风格 | `指令/美学风格/<名字>.md`（可选） |
| 场景 | `背景库/`（仅物理地点；无人物/灯光/镜头） |
| 参考资料 | `参考资料/<来源>/`（沉淀学习素材，只读） |
| 动作 | `动作库/` |
| 对白 | `对白灵感库/` |
| 镜头 | `镜头叙事库/` |
| 模板 | `模板/` |
| 输出 | `创意MV/NNN.txt` |
| 脚本 | `scripts/` |

## 风格与底线

- **风格**：灵活模式，由用户/调度层/AI 共同决定，CLAUDE.md 不再锁定默认画风。
- **真实感硬约束**（与风格无关，永久生效）：详见 `指令/约束.md`。
- **风格禁区**：若需要避免某些风格（如霓虹赛博、直播设备），由具体风格档案或用户显式声明；不在 CLAUDE.md 全局禁用。

## 架构原则

- **多样性来自资产池随机抽样**——不维护跨会话的"已用索引"
- **多样性不靠"比对历史"**，靠"维度矩阵 + 随机"
- **不堆防御**——不为假想问题提前建库
- 主指令保留硬护栏："不读 `创意MV/*.txt` 历史文件"

## 工作语言

- 主指令、文档：中文
- 提示词本体：**默认中文**（Seedance）
- 切换英文 h3：参考 `模板/h3-Subject1-模板.md`
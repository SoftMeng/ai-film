# 模板

输出格式约定。每种模式 / 字段约束独立成文件。

## 当前模板

| 文件 | 用途 |
|---|---|
| `Seedance-中文结构.md` | 当前工程**默认**输出格式（中文 Seedance） |
| `h3-Subject1-模板.md` | 英文 h3 三段式输出时的 `<Subject 1>` 字段模板（用户明确要求时）|

## 默认切换

默认使用 `Seedance-中文结构.md`。仅当用户明确要求英文 h3 / I2VA / Ref2VA 时切换到 `h3-Subject1-模板.md` 等。